using System;

class Program
{
    static void Main(string[] args)
    {
        /*////H13: Foydalanuvchi 10 ta son kiritadi.Shu sonlarning barchasiga 10 ni qo'shib
        ////qo'ying va ekranga chiqaring. Array bilan ishlansin
        //int[] numbers = new int[10];

        //// Kullanıcıdan 10 adet sayı alınması
        //for (int i = 0; i < 10; i++)
        //{
        //    Console.Write($"Sayı {i + 10}: ");
        //    numbers[i] = Convert.ToInt32(Console.ReadLine());
        //}

  

        //// Her bir sayıya 10 eklenmesi
        //for (int i = 0; i < 10; i++)
        //{
        //    numbers[i] += 10;
        //}

        //// Sonuçların ekrana yazdırılması
        //Console.WriteLine("Sonuçlar:");
        //for (int i = 0; i < 10; i++)
        //{
        //    Console.WriteLine(numbers[i]);
        //}

        ////H14:Foydalanuvchi 10 ta son kiritadi.Shu sonlarni arrayga o'zlashtiring.
        ////So'ngra shu array elementlarini yangi arrayga o'zlashtiring,
        ////va shu yangi arrayni ekranga chiqaring
        //int[] arr_number_null = new int[10];
        //int[] numbers = new int[10];
        //for (int i = 0; i < numbers.Length; i++)
        //{
        //    Console.WriteLine($"Son {i+1}");
        //    numbers[i] = int.Parse( Console.ReadLine());
        //}
        //for (int i = 0;i < numbers.Length;i++)
        //{
        //    numbers[i] = arr_number_null[i];
        //}
        //for(int i = 0; i< numbers.Length ; i++)
        //{
        //    Console.WriteLine(arr_number_null[i]);
        //}

        ////H15:Foydalanuvchi 10 ta son kiritadi.Shu arrayning qiymatlari yig'indisini toping.
        ////Note: Array dan boshqa o'zgaruvchi e'lon qilinmasin.
        //int[] numbers = new int[3];
        //for (int i = 0; i < numbers.Length; i++)
        //{
        //    Console.WriteLine($"Son {i+1}");
        //    numbers[i] = int.Parse( Console.ReadLine() );
        //}
        //for (int i = 0;i < numbers.Length;i++)
        //{
        //    numbers[0] += numbers[i];
        //}
        //Console.WriteLine(numbers[0]);



        ////H16:Foydalanuvchi 10 ta son kiritadi.Shu sonlarning musbatlari
        ////yig'indisidan, manfiylari yig'indisini ayiring. Array bilan ishlansin

        //int[] numbers = new int[6];
        //for (int i = 0; i < numbers.Length; i++)
        //{
        //    Console.WriteLine($"Son {i + 1}");
        //    numbers[i] = int.Parse(Console.ReadLine());
        //}
        //for (int i = 0;i < numbers.Length; i++)
        //{
        // int sum1 = 0;
        // int sum2 = 0;
        // if (numbers[i] >= 0)
        // {
        //     sum1 += numbers[i];
        // }
        // else 
        //     sum2 += numbers[i];
        // int all_sum = sum1 - sum2;
        // Console.WriteLine(all_sum);
        //}*/

    }
}
